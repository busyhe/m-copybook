# m-copybook

一个基于 Next.js 15 和 Turborepo 构建的现代中文书法字帖生成器。创建包含拼音、笔顺和多种网格样式的自定义练习册。

[English Documentation](./README.md)

## ✨ 特性

- ✍️ **笔顺支持** - 使用 `hanzi-writer` 可视化汉字笔顺。
- 🔤 **拼音集成** - 使用 `pinyin-pro` 自动为汉字生成拼音。
- 📏 **自定义网格** - 支持多种网格类型：田字格、米字格、回宫格或无网格。
- 🎨 **灵活样式** - 自定义字体（楷体、宋体、黑体）、字号、粗细和颜色。
- 📐 **布局控制** - 调整网格大小、行间距和垂直偏移。
- 📄 **多页 PDF/PNG 导出** - 将练习册导出为高质量的 PDF 或 PNG 文件。
- 🚀 **Next.js 15 & Turbopack** - 基于最新的 Next.js 版本，支持极速开发。
- 🏗️ **Turborepo** - 高性能 Monorepo 架构。

## 🏗️ 项目结构

```
m-copybook/
├── apps/
│   └── web/                 # Next.js 主应用
│       ├── app/            # App Router 页面
│       ├── components/     # 应用级组件 (字帖, UI)
│       └── hooks/          # 自定义 Hooks (Zustand store)
├── packages/
│   ├── ui/                 # 共享 UI 组件库
│   ├── eslint-config/      # 共享 ESLint 配置
│   └── typescript-config/  # 共享 TypeScript 配置
└── ...
```

## 🛠️ 技术栈

### 前端与框架

- **Next.js 15** - React 框架
- **React 19** - UI 库
- **TypeScript** - 类型安全
- **Zustand** - 状态管理

### 书法与语言

- **hanzi-writer** - 汉字笔顺渲染
- **pinyin-pro** - 拼音生成
- **jspdf** - PDF 生成

### 样式

- **Tailwind CSS 4** - 现代 CSS 框架
- **Radix UI** - 无样式组件库
- **Lucide React** - 图标库

### 开发工具

- **Turborepo** - Monorepo 构建工具
- **pnpm** - 包管理器
- **Lefthook** - Git Hooks

## 🚀 快速入门

### 前置条件

- Node.js >= 20
- pnpm >= 10

### 安装

```bash
# 克隆项目
git clone <your-repo-url>
cd m-copybook

# 安装依赖
pnpm install
```

### 开发

```bash
# 启动开发服务器
pnpm dev

# 构建项目
pnpm build
```

访问 [http://localhost:3000](http://localhost:3000) 查看应用。

## 📋 脚本

| 命令             | 描述                |
| ---------------- | ------------------- |
| `pnpm dev`       | 启动开发服务器      |
| `pnpm build`     | 构建所有包          |
| `pnpm lint`      | 运行 ESLint 检查    |
| `pnpm typecheck` | TypeScript 类型检查 |

## 📄 开源协议

[MIT License](LICENSE)
